import React from 'react';

function Sun({ color }) {
  return (
    <div className="sun" style={{ backgroundColor: color }}>
      {/* You can customize the sun's content here */}
    </div>
  );
}

export default Sun;
